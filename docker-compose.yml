version: '3.9'

services:
  frontend:
    build: .
    container_name: runpath_frontend
    ports:
      - '3001:3001'
    env_file:
      - .env
    networks:
      - frontend_net

networks:
  frontend_net:
    external: true
  